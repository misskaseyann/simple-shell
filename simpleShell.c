#include <stdio.h>

/* program start */
int main() {
  /* while(true) */
    /* display_prompt() */
    /* read_command(&command, &params) */
    /* pid = fork() */
    /* if (pid < 0) */
      /* error_routine() */
    /* else if (pid) */
      /* waitpid(-1, &status, 0) */
    /* else */
      /* execve(command, params, 0) */
}
